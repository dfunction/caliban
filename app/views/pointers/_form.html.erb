<%= form_for(@pointer) do |f| %>
  <% if @pointer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pointer.errors.count, "error") %> prohibited this pointer from being saved:</h2>

      <ul>
      <% @pointer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.text_field :contact_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
